{% extends 'base.html.twig' %}

{% block body %}
    <h1>Caso</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ caso.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('caso_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('caso_edit', { 'id': caso.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
