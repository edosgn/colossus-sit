{% extends 'base.html.twig' %}

{% block body %}
    <h1>Banco list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for banco in bancos %}
            <tr>
                <td><a href="{{ path('banco_show', { 'id': banco.id }) }}">{{ banco.id }}</a></td>
                <td>{{ banco.nombre }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('banco_show', { 'id': banco.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('banco_edit', { 'id': banco.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('banco_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
