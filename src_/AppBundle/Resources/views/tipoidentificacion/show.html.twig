{% extends 'base.html.twig' %}

{% block body %}
    <h1>TipoIdentificacion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipoIdentificacion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipoIdentificacion.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipoidentificacion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tipoidentificacion_edit', { 'id': tipoIdentificacion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
