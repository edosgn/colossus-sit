{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cfgsvsenialestado</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cfgSvSenialEstado.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cfgSvSenialEstado.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if cfgSvSenialEstado.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cfgsvsenialestado_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cfgsvsenialestado_edit', { 'id': cfgSvSenialEstado.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
