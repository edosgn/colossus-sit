{% extends 'base.html.twig' %}

{% block body %}
    <h1>Mfltiporecaudo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mflTipoRecaudo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ mflTipoRecaudo.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if mflTipoRecaudo.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('mfltiporecaudo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('mfltiporecaudo_edit', { 'id': mflTipoRecaudo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
