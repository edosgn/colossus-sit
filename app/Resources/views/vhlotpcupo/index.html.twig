{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vhlotpcupos list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for vhloTpCupo in vhloTpCupos %}
            <tr>
                <td><a href="{{ path('vhlotpcupo_show', { 'id': vhloTpCupo.id }) }}">{{ vhloTpCupo.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('vhlotpcupo_show', { 'id': vhloTpCupo.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
