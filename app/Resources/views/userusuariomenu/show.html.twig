{% extends 'base.html.twig' %}

{% block body %}
    <h1>Userusuariomenu</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ userUsuarioMenu.id }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if userUsuarioMenu.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('userusuariomenu_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('userusuariomenu_edit', { 'id': userUsuarioMenu.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
