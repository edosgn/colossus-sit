{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgestadotiempos list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgEstadoTiempo in svCfgEstadoTiempos %}
            <tr>
                <td><a href="{{ path('svcfgestadotiempo_show', { 'id': svCfgEstadoTiempo.id }) }}">{{ svCfgEstadoTiempo.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgestadotiempo_show', { 'id': svCfgEstadoTiempo.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
