{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cfgempresagp</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cfgEmpresaGp.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cfgEmpresaGp.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ cfgEmpresaGp.estado }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cfgempresagps_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cfgempresagps_edit', { 'id': cfgEmpresaGp.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
