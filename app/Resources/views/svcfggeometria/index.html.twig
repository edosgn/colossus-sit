{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfggeometrias list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgGeometrium in svCfgGeometrias %}
            <tr>
                <td><a href="{{ path('svcfggeometria_show', { 'id': svCfgGeometrium.id }) }}">{{ svCfgGeometrium.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfggeometria_show', { 'id': svCfgGeometrium.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
