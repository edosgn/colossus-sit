{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgclaseaccidente</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ svCfgClaseAccidente.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ svCfgClaseAccidente.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if svCfgClaseAccidente.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('svcfgclaseaccidente_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('svcfgclaseaccidente_edit', { 'id': svCfgClaseAccidente.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
