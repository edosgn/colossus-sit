{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgiluminacions list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgIluminacion in svCfgIluminacions %}
            <tr>
                <td><a href="{{ path('svcfgiluminacion_show', { 'id': svCfgIluminacion.id }) }}">{{ svCfgIluminacion.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgiluminacion_show', { 'id': svCfgIluminacion.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
