{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cfgtipoalertum</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cfgTipoAlertum.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cfgTipoAlertum.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if cfgTipoAlertum.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cfgtipoalerta_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cfgtipoalerta_edit', { 'id': cfgTipoAlertum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
