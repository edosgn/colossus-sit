{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgareas list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgArea in svCfgAreas %}
            <tr>
                <td><a href="{{ path('svcfgarea_show', { 'id': svCfgArea.id }) }}">{{ svCfgArea.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgarea_show', { 'id': svCfgArea.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
