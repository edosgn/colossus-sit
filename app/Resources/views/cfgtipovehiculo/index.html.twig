{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cfgtipovehiculos list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cfgTipoVehiculo in cfgTipoVehiculos %}
            <tr>
                <td><a href="{{ path('cfgtipovehiculo_show', { 'id': cfgTipoVehiculo.id }) }}">{{ cfgTipoVehiculo.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('cfgtipovehiculo_show', { 'id': cfgTipoVehiculo.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
