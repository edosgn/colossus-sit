{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgrequiereempresas list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgRequiereEmpresa in svCfgRequiereEmpresas %}
            <tr>
                <td><a href="{{ path('svcfgrequiereempresa_show', { 'id': svCfgRequiereEmpresa.id }) }}">{{ svCfgRequiereEmpresa.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgrequiereempresa_show', { 'id': svCfgRequiereEmpresa.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
