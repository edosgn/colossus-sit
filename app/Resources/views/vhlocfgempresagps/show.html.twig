{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vhlocfgempresagp</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vhloCfgEmpresaGp.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ vhloCfgEmpresaGp.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ vhloCfgEmpresaGp.activo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vhlocfgempresagps_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vhlocfgempresagps_edit', { 'id': vhloCfgEmpresaGp.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
