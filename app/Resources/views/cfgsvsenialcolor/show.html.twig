{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cfgsvsenialcolor</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cfgSvSenialColor.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cfgSvSenialColor.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if cfgSvSenialColor.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cfgsvsenialcolor_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cfgsvsenialcolor_edit', { 'id': cfgSvSenialColor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
