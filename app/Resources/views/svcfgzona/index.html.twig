{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgzonas list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgZona in svCfgZonas %}
            <tr>
                <td><a href="{{ path('svcfgzona_show', { 'id': svCfgZona.id }) }}">{{ svCfgZona.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgzona_show', { 'id': svCfgZona.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
