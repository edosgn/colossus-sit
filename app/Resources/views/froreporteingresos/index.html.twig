{% extends 'base.html.twig' %}

{% block body %}
    <h1>Froreporteingresos list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for froReporteIngreso in froReporteIngresos %}
            <tr>
                <td><a href="{{ path('froreporteingresos_show', { 'id': froReporteIngreso.id }) }}">{{ froReporteIngreso.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('froreporteingresos_show', { 'id': froReporteIngreso.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
