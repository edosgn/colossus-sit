{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgsenialestado</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ svCfgSenialEstado.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ svCfgSenialEstado.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if svCfgSenialEstado.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('svcfgsenialestado_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('svcfgsenialestado_edit', { 'id': svCfgSenialEstado.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
