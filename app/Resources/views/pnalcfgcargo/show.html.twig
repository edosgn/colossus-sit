{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pnalcfgcargo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pnalCfgCargo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ pnalCfgCargo.nombre }}</td>
            </tr>
            <tr>
                <th>Gestionable</th>
                <td>{% if pnalCfgCargo.gestionable %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if pnalCfgCargo.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pnalcfgcargo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('pnalcfgcargo_edit', { 'id': pnalCfgCargo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
