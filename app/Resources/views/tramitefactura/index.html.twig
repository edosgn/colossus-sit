{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tramitefacturas list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tramiteFactura in tramiteFacturas %}
            <tr>
                <td><a href="{{ path('tramitefactura_show', { 'id': tramiteFactura.id }) }}">{{ tramiteFactura.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('tramitefactura_show', { 'id': tramiteFactura.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
