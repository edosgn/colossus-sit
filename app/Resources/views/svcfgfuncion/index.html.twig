{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgfunciones list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgFuncion in svCfgFunciones %}
            <tr>
                <td><a href="{{ path('svcfgfuncion_show', { 'id': svCfgFuncion.id }) }}">{{ svCfgFuncion.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgfuncion_show', { 'id': svCfgFuncion.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
