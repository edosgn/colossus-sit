{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgaseguradoras list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgAseguradora in svCfgAseguradoras %}
            <tr>
                <td><a href="{{ path('svcfgaseguradora_show', { 'id': svCfgAseguradora.id }) }}">{{ svCfgAseguradora.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgaseguradora_show', { 'id': svCfgAseguradora.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
