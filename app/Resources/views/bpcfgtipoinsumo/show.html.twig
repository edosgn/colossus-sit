{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bpcfgtipoinsumo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bpCfgTipoInsumo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ bpCfgTipoInsumo.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if bpCfgTipoInsumo.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bpcfgtipoinsumo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bpcfgtipoinsumo_edit', { 'id': bpCfgTipoInsumo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
