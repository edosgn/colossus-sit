{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgsolidaridads list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgSolidaridad in svCfgSolidaridads %}
            <tr>
                <td><a href="{{ path('svcfgsolidaridad_show', { 'id': svCfgSolidaridad.id }) }}">{{ svCfgSolidaridad.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgsolidaridad_show', { 'id': svCfgSolidaridad.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
