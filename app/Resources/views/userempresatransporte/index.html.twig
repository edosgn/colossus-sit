{% extends 'base.html.twig' %}

{% block body %}
    <h1>Userempresatransportes list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for userEmpresaTransporte in userEmpresaTransportes %}
            <tr>
                <td><a href="{{ path('userempresatransporte_show', { 'id': userEmpresaTransporte.id }) }}">{{ userEmpresaTransporte.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('userempresatransporte_show', { 'id': userEmpresaTransporte.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
