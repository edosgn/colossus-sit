{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgunidadreceptoras list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgUnidadReceptora in svCfgUnidadReceptoras %}
            <tr>
                <td><a href="{{ path('svcfgunidadreceptora_show', { 'id': svCfgUnidadReceptora.id }) }}">{{ svCfgUnidadReceptora.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgunidadreceptora_show', { 'id': svCfgUnidadReceptora.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
