{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vhlolimitacion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vhloLimitacion.id }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if vhloLimitacion.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vhlolimitacion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vhlolimitacion_edit', { 'id': vhloLimitacion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
