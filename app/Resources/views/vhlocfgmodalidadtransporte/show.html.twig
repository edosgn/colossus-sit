{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vhlocfgmodalidadtransporte</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vhloCfgModalidadTransporte.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ vhloCfgModalidadTransporte.nombre }}</td>
            </tr>
            <tr>
                <th>Gestionable</th>
                <td>{% if vhloCfgModalidadTransporte.gestionable %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if vhloCfgModalidadTransporte.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vhlocfgmodalidadtransporte_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vhlocfgmodalidadtransporte_edit', { 'id': vhloCfgModalidadTransporte.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
