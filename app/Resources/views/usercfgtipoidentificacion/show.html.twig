{% extends 'base.html.twig' %}

{% block body %}
    <h1>Usercfgtipoidentificacion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ userCfgTipoIdentificacion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ userCfgTipoIdentificacion.nombre }}</td>
            </tr>
            <tr>
                <th>Sigla</th>
                <td>{{ userCfgTipoIdentificacion.sigla }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if userCfgTipoIdentificacion.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('usercfgtipoidentificacion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('usercfgtipoidentificacion_edit', { 'id': userCfgTipoIdentificacion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
