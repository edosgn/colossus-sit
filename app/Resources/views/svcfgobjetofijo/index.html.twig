{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgobjetofijos list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgObjetoFijo in svCfgObjetoFijos %}
            <tr>
                <td><a href="{{ path('svcfgobjetofijo_show', { 'id': svCfgObjetoFijo.id }) }}">{{ svCfgObjetoFijo.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgobjetofijo_show', { 'id': svCfgObjetoFijo.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
