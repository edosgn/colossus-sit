{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgnacionalidads list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgNacionalidad in svCfgNacionalidads %}
            <tr>
                <td><a href="{{ path('svcfgnacionalidad_show', { 'id': svCfgNacionalidad.id }) }}">{{ svCfgNacionalidad.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgnacionalidad_show', { 'id': svCfgNacionalidad.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
