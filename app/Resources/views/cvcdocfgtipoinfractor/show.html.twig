{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cvcdocfgtipoinfractor</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cvCdoCfgTipoInfractor.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cvCdoCfgTipoInfractor.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if cvCdoCfgTipoInfractor.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cvcdocfgtipoinfractor_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cvcdocfgtipoinfractor_edit', { 'id': cvCdoCfgTipoInfractor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
