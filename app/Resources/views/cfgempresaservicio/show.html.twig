{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cfgempresaservicio</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cfgEmpresaServicio.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cfgEmpresaServicio.nombre }}</td>
            </tr>
            <tr>
                <th>Gestionable</th>
                <td>{% if cfgEmpresaServicio.gestionable %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if cfgEmpresaServicio.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cfgempresaservicio_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cfgempresaservicio_edit', { 'id': cfgEmpresaServicio.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
