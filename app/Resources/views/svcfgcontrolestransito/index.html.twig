{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgcontrolestransitos list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgControlesTransito in svCfgControlesTransitos %}
            <tr>
                <td><a href="{{ path('svcfgcontrolestransito_show', { 'id': svCfgControlesTransito.id }) }}">{{ svCfgControlesTransito.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgcontrolestransito_show', { 'id': svCfgControlesTransito.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
