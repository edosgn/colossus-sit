{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cvlccfgrestriccion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cvLcCfgRestriccion.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ cvLcCfgRestriccion.descripcion }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if cvLcCfgRestriccion.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cvlccfgrestriccion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cvlccfgrestriccion_edit', { 'id': cvLcCfgRestriccion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
