{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgcontrolvias list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgControlVium in svCfgControlVias %}
            <tr>
                <td><a href="{{ path('svcfgcontrolvia_show', { 'id': svCfgControlVium.id }) }}">{{ svCfgControlVium.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgcontrolvia_show', { 'id': svCfgControlVium.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
