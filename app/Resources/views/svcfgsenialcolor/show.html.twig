{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgsenialcolor</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ svCfgSenialColor.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ svCfgSenialColor.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if svCfgSenialColor.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('svcfgsenialcolor_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('svcfgsenialcolor_edit', { 'id': svCfgSenialColor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
