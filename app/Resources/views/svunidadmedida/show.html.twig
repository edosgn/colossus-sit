{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svunidadmedida</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ svUnidadMedida.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ svUnidadMedida.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if svUnidadMedida.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('svunidadmedida_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('svunidadmedida_edit', { 'id': svUnidadMedida.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
