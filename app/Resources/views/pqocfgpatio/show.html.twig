{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pqocfgpatio</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pqoCfgPatio.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ pqoCfgPatio.nombre }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ pqoCfgPatio.direccion }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if pqoCfgPatio.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pqocfgpatio_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('pqocfgpatio_edit', { 'id': pqoCfgPatio.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
