{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cfgentidadjudicial</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cfgEntidadJudicial.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cfgEntidadJudicial.nombre }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ cfgEntidadJudicial.codigo }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if cfgEntidadJudicial.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cfgentidadjudicial_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cfgentidadjudicial_edit', { 'id': cfgEntidadJudicial.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
