{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vhlocfgmotivocancelacion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vhloCfgMotivoCancelacion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ vhloCfgMotivoCancelacion.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if vhloCfgMotivoCancelacion.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Gestionable</th>
                <td>{% if vhloCfgMotivoCancelacion.gestionable %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vhlocfgmotivocancelacion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vhlocfgmotivocancelacion_edit', { 'id': vhloCfgMotivoCancelacion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
