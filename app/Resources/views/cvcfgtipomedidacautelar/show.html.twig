{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cvcfgtipomedidacautelar</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cvCfgTipoMedidaCautelar.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cvCfgTipoMedidaCautelar.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if cvCfgTipoMedidaCautelar.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cvcfgtipomedidacautelar_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cvcfgtipomedidacautelar_edit', { 'id': cvCfgTipoMedidaCautelar.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
