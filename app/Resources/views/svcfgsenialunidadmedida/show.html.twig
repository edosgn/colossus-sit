{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgsenialunidadmedida</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ svCfgSenialUnidadMedida.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ svCfgSenialUnidadMedida.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if svCfgSenialUnidadMedida.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('svcfgsenialunidadmedida_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('svcfgsenialunidadmedida_edit', { 'id': svCfgSenialUnidadMedida.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
