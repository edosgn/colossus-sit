{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svsenialinventariomunicipio</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ svSenialInventarioMunicipio.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{% if svSenialInventarioMunicipio.fecha %}{{ svSenialInventarioMunicipio.fecha|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Consecutivo</th>
                <td>{{ svSenialInventarioMunicipio.consecutivo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('svsenialinventariomunicipio_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('svsenialinventariomunicipio_edit', { 'id': svSenialInventarioMunicipio.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
