{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgsenialconector</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ svCfgSenialConector.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ svCfgSenialConector.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if svCfgSenialConector.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('svcfgsenialconector_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('svcfgsenialconector_edit', { 'id': svCfgSenialConector.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
