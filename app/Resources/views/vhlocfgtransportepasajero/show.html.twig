{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vhlocfgtransportepasajero</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vhloCfgTransportePasajero.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ vhloCfgTransportePasajero.nombre }}</td>
            </tr>
            <tr>
                <th>Gestionable</th>
                <td>{% if vhloCfgTransportePasajero.gestionable %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if vhloCfgTransportePasajero.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vhlocfgtransportepasajero_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vhlocfgtransportepasajero_edit', { 'id': vhloCfgTransportePasajero.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
