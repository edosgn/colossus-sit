{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vhlotpasignacions list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for vhloTpAsignacion in vhloTpAsignacions %}
            <tr>
                <td><a href="{{ path('vhlotpasignacion_show', { 'id': vhloTpAsignacion.id }) }}">{{ vhloTpAsignacion.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('vhlotpasignacion_show', { 'id': vhloTpAsignacion.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
