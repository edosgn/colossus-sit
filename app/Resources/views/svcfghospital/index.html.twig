{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfghospitals list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgHospital in svCfgHospitals %}
            <tr>
                <td><a href="{{ path('svcfghospital_show', { 'id': svCfgHospital.id }) }}">{{ svCfgHospital.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfghospital_show', { 'id': svCfgHospital.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
