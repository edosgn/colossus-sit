{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cfgcomparendoestado</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cfgComparendoEstado.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cfgComparendoEstado.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if cfgComparendoEstado.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cfgcomparendoestado_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cfgcomparendoestado_edit', { 'id': cfgComparendoEstado.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
