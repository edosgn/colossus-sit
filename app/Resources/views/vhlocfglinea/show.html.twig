{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vhlocfglinea</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vhloCfgLinea.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ vhloCfgLinea.nombre }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ vhloCfgLinea.codigo }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if vhloCfgLinea.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vhlocfglinea_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vhlocfglinea_edit', { 'id': vhloCfgLinea.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
