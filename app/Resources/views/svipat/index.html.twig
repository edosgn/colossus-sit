{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svipats list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svIpat in svIpats %}
            <tr>
                <td><a href="{{ path('svipat_show', { 'id': svIpat.id }) }}">{{ svIpat.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svipat_show', { 'id': svIpat.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
