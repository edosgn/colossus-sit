{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgpreventivas list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgPreventiva in svCfgPreventivas %}
            <tr>
                <td><a href="{{ path('svcfgpreventiva_show', { 'id': svCfgPreventiva.id }) }}">{{ svCfgPreventiva.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgpreventiva_show', { 'id': svCfgPreventiva.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
