{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgfallas list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgFalla in svCfgFallas %}
            <tr>
                <td><a href="{{ path('svcfgfalla_show', { 'id': svCfgFalla.id }) }}">{{ svCfgFalla.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgfalla_show', { 'id': svCfgFalla.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
