{% extends 'base.html.twig' %}

{% block body %}
    <h1>Origenregistro</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ origenRegistro.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ origenRegistro.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if origenRegistro.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('origenregistro_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('origenregistro_edit', { 'id': origenRegistro.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
