{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgutilizacions list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgUtilizacion in svCfgUtilizacions %}
            <tr>
                <td><a href="{{ path('svcfgutilizacion_show', { 'id': svCfgUtilizacion.id }) }}">{{ svCfgUtilizacion.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgutilizacion_show', { 'id': svCfgUtilizacion.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
