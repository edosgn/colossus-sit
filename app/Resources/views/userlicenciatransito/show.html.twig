{% extends 'base.html.twig' %}

{% block body %}
    <h1>Userlicenciatransito</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ userLicenciaTransito.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ userLicenciaTransito.numero }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{% if userLicenciaTransito.fecha %}{{ userLicenciaTransito.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('userlicenciatransito_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('userlicenciatransito_edit', { 'id': userLicenciaTransito.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
