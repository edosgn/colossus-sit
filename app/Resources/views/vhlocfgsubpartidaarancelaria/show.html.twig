{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vhlocfgsubpartidaarancelarium</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vhloCfgSubpartidaArancelarium.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ vhloCfgSubpartidaArancelarium.codigo }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if vhloCfgSubpartidaArancelarium.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vhlocfgsubpartidaarancelaria_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vhlocfgsubpartidaarancelaria_edit', { 'id': vhloCfgSubpartidaArancelarium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
