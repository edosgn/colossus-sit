{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgmaterials list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgMaterial in svCfgMaterials %}
            <tr>
                <td><a href="{{ path('svcfgmaterial_show', { 'id': svCfgMaterial.id }) }}">{{ svCfgMaterial.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgmaterial_show', { 'id': svCfgMaterial.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
