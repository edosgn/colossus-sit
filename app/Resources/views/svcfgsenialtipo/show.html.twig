{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgsenialtipo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ svCfgSenialTipo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ svCfgSenialTipo.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if svCfgSenialTipo.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('svcfgsenialtipo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('svcfgsenialtipo_edit', { 'id': svCfgSenialTipo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
