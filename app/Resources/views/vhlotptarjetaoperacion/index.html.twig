{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vhlotptarjetaoperacions list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for vhloTpTarjetaOperacion in vhloTpTarjetaOperacions %}
            <tr>
                <td><a href="{{ path('vhlotptarjetaoperacion_show', { 'id': vhloTpTarjetaOperacion.id }) }}">{{ vhloTpTarjetaOperacion.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('vhlotptarjetaoperacion_show', { 'id': vhloTpTarjetaOperacion.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
