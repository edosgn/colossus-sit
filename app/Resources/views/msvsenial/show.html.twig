{% extends 'base.html.twig' %}

{% block body %}
    <h1>MsvSenial</h1>

    <table>
        <tbody>
            <tr>
                <th>Xdestino</th>
                <td>{{ msvSenial.xDestino }}</td>
            </tr>
            <tr>
                <th>MsvSenialid</th>
                <td>{{ msvSenial.msvSenialId }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ msvSenial.id }}</td>
            </tr>
			<tr>
                <th>Archivo</th>
                <td>{{ mrfsvHu08.archivo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('msvSenial_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('msvSenial_edit', { 'id': msvSenial.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
