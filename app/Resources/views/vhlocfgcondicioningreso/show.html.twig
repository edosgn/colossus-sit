{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vhlocfgcondicioningreso</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vhloCfgCondicionIngreso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ vhloCfgCondicionIngreso.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if vhloCfgCondicionIngreso.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vhlocfgcondicioningreso_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vhlocfgcondicioningreso_edit', { 'id': vhloCfgCondicionIngreso.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
