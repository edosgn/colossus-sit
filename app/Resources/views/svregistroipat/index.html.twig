{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svregistroipats list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svRegistroIpat in svRegistroIpats %}
            <tr>
                <td><a href="{{ path('svregistroipat_show', { 'id': svRegistroIpat.id }) }}">{{ svRegistroIpat.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svregistroipat_show', { 'id': svRegistroIpat.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
