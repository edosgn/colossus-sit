{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cfgsvconector</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cfgSvConector.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cfgSvConector.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if cfgSvConector.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cfgsvconector_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cfgsvconector_edit', { 'id': cfgSvConector.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
