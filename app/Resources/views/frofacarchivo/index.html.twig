{% extends 'base.html.twig' %}

{% block body %}
    <h1>Frofacarchivos list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for froFacArchivo in froFacArchivos %}
            <tr>
                <td><a href="{{ path('frofacarchivo_show', { 'id': froFacArchivo.id }) }}">{{ froFacArchivo.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('frofacarchivo_show', { 'id': froFacArchivo.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
