{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgestadoconductors list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgEstadoConductor in svCfgEstadoConductors %}
            <tr>
                <td><a href="{{ path('svcfgestadoconductor_show', { 'id': svCfgEstadoConductor.id }) }}">{{ svCfgEstadoConductor.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgestadoconductor_show', { 'id': svCfgEstadoConductor.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
