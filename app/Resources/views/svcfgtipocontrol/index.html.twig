{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgtipocontrols list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgTipoControl in svCfgTipoControls %}
            <tr>
                <td><a href="{{ path('svcfgtipocontrol_show', { 'id': svCfgTipoControl.id }) }}">{{ svCfgTipoControl.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgtipocontrol_show', { 'id': svCfgTipoControl.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
