{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgeducacions list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgEducacion in svCfgEducacions %}
            <tr>
                <td><a href="{{ path('svcfgeducacion_show', { 'id': svCfgEducacion.id }) }}">{{ svCfgEducacion.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgeducacion_show', { 'id': svCfgEducacion.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
