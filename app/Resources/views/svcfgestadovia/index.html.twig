{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgestadovias list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgEstadoVium in svCfgEstadoVias %}
            <tr>
                <td><a href="{{ path('svcfgestadovia_show', { 'id': svCfgEstadoVium.id }) }}">{{ svCfgEstadoVium.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgestadovia_show', { 'id': svCfgEstadoVium.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
