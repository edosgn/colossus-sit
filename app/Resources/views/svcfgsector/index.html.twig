{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgsectors list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgSector in svCfgSectors %}
            <tr>
                <td><a href="{{ path('svcfgsector_show', { 'id': svCfgSector.id }) }}">{{ svCfgSector.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgsector_show', { 'id': svCfgSector.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
