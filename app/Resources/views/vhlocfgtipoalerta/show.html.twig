{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vhlocfgtipoalertum</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vhloCfgTipoAlertum.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ vhloCfgTipoAlertum.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if vhloCfgTipoAlertum.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vhlocfgtipoalerta_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vhlocfgtipoalerta_edit', { 'id': vhloCfgTipoAlertum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
