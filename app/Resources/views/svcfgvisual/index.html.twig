{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgvisuals list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgVisual in svCfgVisuals %}
            <tr>
                <td><a href="{{ path('svcfgvisual_show', { 'id': svCfgVisual.id }) }}">{{ svCfgVisual.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgvisual_show', { 'id': svCfgVisual.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
