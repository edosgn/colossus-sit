{% extends 'base.html.twig' %}

{% block body %}
    <h1>Svcfgtipoareas list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for svCfgTipoArea in svCfgTipoAreas %}
            <tr>
                <td><a href="{{ path('svcfgtipoarea_show', { 'id': svCfgTipoArea.id }) }}">{{ svCfgTipoArea.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('svcfgtipoarea_show', { 'id': svCfgTipoArea.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
