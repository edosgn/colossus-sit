{% extends 'base.html.twig' %}

{% block body %}
    <h1>Combustible list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Codigomt</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for combustible in combustibles %}
            <tr>
                <td><a href="{{ path('combustible_show', { 'id': combustible.id }) }}">{{ combustible.id }}</a></td>
                <td>{{ combustible.nombre }}</td>
                <td>{{ combustible.codigoMt }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('combustible_show', { 'id': combustible.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('combustible_edit', { 'id': combustible.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('combustible_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
