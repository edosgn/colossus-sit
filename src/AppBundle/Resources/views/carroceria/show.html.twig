{% extends 'base.html.twig' %}

{% block body %}
    <h1>Carroceria</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carrocerium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ carrocerium.nombre }}</td>
            </tr>
            <tr>
                <th>Codigomt</th>
                <td>{{ carrocerium.codigoMt }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('carroceria_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('carroceria_edit', { 'id': carrocerium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
