{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modulo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ modulo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ modulo.nombre }}</td>
            </tr>
            <tr>
                <th>Abreviatura</th>
                <td>{{ modulo.abreviatura }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('modulo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('modulo_edit', { 'id': modulo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
