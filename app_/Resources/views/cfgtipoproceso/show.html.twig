{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cfgtipoproceso</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cfgTipoProceso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cfgTipoProceso.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cfgtipoproceso_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cfgtipoproceso_edit', { 'id': cfgTipoProceso.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
