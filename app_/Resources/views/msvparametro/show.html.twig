{% extends 'base.html.twig' %}

{% block body %}
    <h1>Msvparametro</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ msvParametro.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ msvParametro.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if msvParametro.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Valor</th>
                <td>{{ msvParametro.valor }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('msvparametro_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('msvparametro_edit', { 'id': msvParametro.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
