{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tramiteprecio</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tramitePrecio.id }}</td>
            </tr>
            <tr>
                <th>Valor</th>
                <td>{{ tramitePrecio.valor }}</td>
            </tr>
            <tr>
                <th>Ano</th>
                <td>{{ tramitePrecio.ano }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ tramitePrecio.tipo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tramiteprecio_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tramiteprecio_edit', { 'id': tramitePrecio.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
