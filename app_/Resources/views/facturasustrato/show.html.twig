{% extends 'base.html.twig' %}

{% block body %}
    <h1>Facturasustrato</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facturaSustrato.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('facturasustrato_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('facturasustrato_edit', { 'id': facturaSustrato.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
