{% extends 'base.html.twig' %}

{% block body %}
    <h1>Msvtconsecutivo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ msvTConsecutivo.id }}</td>
            </tr>
            <tr>
                <th>Consecutivo</th>
                <td>{{ msvTConsecutivo.consecutivo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('msvtconsecutivo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('msvtconsecutivo_edit', { 'id': msvTConsecutivo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
