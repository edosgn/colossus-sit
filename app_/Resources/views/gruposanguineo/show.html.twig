{% extends 'base.html.twig' %}

{% block body %}
    <h1>Gruposanguineo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grupoSanguineo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ grupoSanguineo.nombre }}</td>
            </tr>
            <tr>
                <th>Sigla</th>
                <td>{{ grupoSanguineo.sigla }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if grupoSanguineo.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('gruposanguineo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('gruposanguineo_edit', { 'id': grupoSanguineo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
