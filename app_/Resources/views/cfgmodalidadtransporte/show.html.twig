{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cfgmodalidadtransporte</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cfgModalidadTransporte.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cfgModalidadTransporte.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if cfgModalidadTransporte.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cfgmodalidadtransporte_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cfgmodalidadtransporte_edit', { 'id': cfgModalidadTransporte.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
