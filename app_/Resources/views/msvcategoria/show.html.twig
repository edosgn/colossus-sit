{% extends 'base.html.twig' %}

{% block body %}
    <h1>Msvcategorium</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ msvCategorium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ msvCategorium.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if msvCategorium.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('msvcategoria_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('msvcategoria_edit', { 'id': msvCategorium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
