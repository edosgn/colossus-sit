{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tiposenal</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ tipoSenal.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if tipoSenal.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ tipoSenal.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tiposenal_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tiposenal_edit', { 'id': tipoSenal.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
