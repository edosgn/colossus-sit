{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tipocorrespondencium</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipoCorrespondencium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipoCorrespondencium.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if tipoCorrespondencium.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipocorrespondencia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tipocorrespondencia_edit', { 'id': tipoCorrespondencium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
